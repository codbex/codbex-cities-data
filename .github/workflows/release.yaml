name: Release - Module

on:
  workflow_dispatch:
    inputs:
      release-version:
        description: Release Version
        required: true
        default: 1.0.0

run-name: 'version set to ${{ inputs.release-version }} for release'

jobs:
  main:
    uses: codbex/codbex-infra/.github/workflows/module-release.yaml@main
    with:
      packages: codbex-cities-data
      packages-build: codbex-cities-data
      release-version: ${{ inputs.release-version }}
      release-content: |
        ## codbex-cities-data - ${{ inputs.release-version }}
  
        Currencies Data Module
  
        ## Installation
  
        ```
        npm install @codbex/codbex-cities-data@${{ inputs.release-version }}
        ```
  
    secrets: inherit
